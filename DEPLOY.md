# HandzJ deploy

## Product screens
Cropped live screenshots (browser chrome + taskbar removed):
- `assets/product/home-receipt-*` — receipt VALID panel
- `assets/product/login-*`
- `assets/product/signup-*`

Responsive: 640 / 960 / 1280 · JPEG + WebP

## Apply
1. SQL: `025` then `026`
2. Merge `public/` into site
3. Confirm homepage shows UI-only frames (no address bar / taskbar)
